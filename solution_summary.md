# 设备定位信息解析问题解决方案

## 问题描述
设备的定位信息保存时，没有将原始数据中的经纬度分离出来保存，同时其他数据也没有正确解析。原始数据格式示例：
```
{raw=251212V0000.0000N00000.0000E000.0073042000.0006000008400008,460,04,32845,236380872,AP1|d2:82:3d:a7:e2:ce|-46&AP2|5c:02:14:b3:4d:56|-53&AP3|5e:02:14:a3:4d:56|-53&AP4|52:74:8d:c7:73:a6|-69&AP5|06:05:88:1e:6d:ba|-81,[30.886866@103.5945]}
```

## 问题分析
通过查看代码，发现定位数据处理逻辑存在以下问题：
1. 设备发送的定位数据带有 `raw=` 前缀
2. 系统直接将带有前缀的数据作为GPS部分处理，导致无法正确解析经纬度
3. 数据保存到数据库时，经纬度、速度、方向等字段均为NULL

## 解决方案
在 `LocationProtocolHandler.java` 文件中添加处理 `raw=` 前缀的逻辑，具体修改如下：

### 修改位置
**文件路径**：`e:\smart\master\src\main\java\com\example\demo\socket\protocol\LocationProtocolHandler.java`

### 修改内容
在解析GPS数据前，添加处理 `raw=` 前缀的逻辑：
```java
// 处理带有 raw= 前缀的数据格式
String processedPayload = payload;
if (processedPayload.startsWith("raw=")) {
    processedPayload = processedPayload.substring(4);
}
```

### 工作原理
1. 当设备发送带有 `raw=` 前缀的定位数据时
2. 系统先去掉 `raw=` 前缀，获取实际的GPS数据
3. 然后按照正常逻辑解析经纬度、速度、方向等信息
4. 最后将解析后的数据保存到数据库中

## 验证结果
1. 编译代码：`mvn clean compile` - 成功，无编译错误
2. 运行测试：`mvn test -Dtest=ProtocolParserTest` - 成功，所有12个测试用例通过

## 预期效果
修改后，系统将能够正确解析带有 `raw=` 前缀的定位数据，具体表现为：
1. `latitude` 和 `longitude` 字段将保存解析后的经纬度
2. `speed` 和 `direction` 字段将保存解析后的速度和方向
3. 其他相关字段也将正确填充
4. 原始数据仍将完整保存到 `extra_raw` 字段中，便于调试和二次解析

## 数据流向
修改后的完整数据流向：
1. 设备发送定位数据（带有 `raw=` 前缀）
2. 服务器接收数据，去掉 `raw=` 前缀
3. 解析GPS数据，提取经纬度、速度、方向等信息
4. 保存解析后的数据到数据库中
5. 同时保存原始数据到 `extra_raw` 字段

## 总结
本次修改解决了设备定位信息解析问题，使系统能够正确处理带有 `raw=` 前缀的定位数据，将经纬度等信息分离出来保存到数据库中。修改简单、高效，且不影响现有功能。